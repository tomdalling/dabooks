#!/bin/bash
bundle exec ruby -I lib/ -I test/ -x "$0" "$@"
exit

#!ruby
require 'minitest/autorun'

tests = (ARGV.empty? ? Dir['test/**/*_test.rb'] : ARGV)
tests.each do |path|
  require File.expand_path(path)
end
